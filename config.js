/**
 * Lumina Electric Air — Frontend configuration
 * ---------------------------------------------
 * After you deploy google-apps-script/Code.gs as a Web App (see README.md,
 * step "Deploy the Apps Script"), paste the deployment URL below.
 *
 * It looks like:
 * https://script.google.com/macros/s/AKfycbx.../exec
 *
 * This is a public-facing URL by design (Apps Script Web Apps work this
 * way) — it is not a secret. The passcodes that protect the results
 * dashboard live only in the Apps Script's Script Properties, never here.
 */
const LUMINA_CONFIG = {
  APPS_SCRIPT_URL: "PASTE_YOUR_DEPLOYED_WEB_APP_URL_HERE"
};
